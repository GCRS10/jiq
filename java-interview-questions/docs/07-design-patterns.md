# Design Patterns

## Thread-Safe Singleton
Implement the Singleton pattern with lazy, thread-safe initialization using
the initialization-on-demand holder idiom (plus a double-checked-locking
variant for comparison).

**File:** `src/main/java/com/interview/design/Singleton.java`

## Observer Pattern
Implement the Observer pattern: a subject that notifies subscribed observers
whenever its state changes.

**File:** `src/main/java/com/interview/design/ObserverPattern.java`
